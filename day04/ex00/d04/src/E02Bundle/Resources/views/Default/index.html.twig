<h1>Submit your message</h1>

{{ form_start(form) }}
    {{ form_row(form.message) }}
    {{ form_row(form.include_timestamp) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}

{% if last_line %}
    <h3>Last saved line:</h3>
    <p>{{ last_line }}</p>
{% endif %}
